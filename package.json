{
    "scripts": {
        "start": "cd server && npm run bots && cd .. && concurrently -n overlay,server,runner \"cd overlay && npm run buildpreview\" \"cd server && npm run start\" \"cd ultimaterunner && npm run start\"",
        "dev": "cd server && npm run bots && cd ..  && concurrently -n overlay,server,runner \"cd overlay && npm run dev\" \"cd server && npm run dev\" \"cd ultimaterunner && npm run dev\""
    },
    "dependencies": {
        "concurrently": "^6.0.1"
    }
}
